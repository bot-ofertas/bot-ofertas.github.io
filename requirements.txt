python-dotenv
python-telegram-bot>=20.0
flask
requests
playwright
anthropic>=0.40.0
Pillow>=10.1.0
tweepy>=4.14.0
pyautogui
pygetwindow
pyperclip
instagrapi
psutil
telethon>=1.44.0
pywinauto
python-amazon-paapi>=6.3.0

# --- Acrescentados em 2026-09-08 -------------------------------------------
# O PC do Daniel ficou sem Python (limpeza de sistema). Reinstalar e rodar
# `pip install -r requirements.txt` NAO devolvia o bot ao ar, porque estes
# tres eram importados pelo codigo e nunca estiveram na lista.

# pywin32 traz win32clipboard/win32api/win32con/win32event/win32process.
# Sem ele, `_copiar_arquivo_clipboard` devolve False, a foto nao chega ao
# clipboard e o envio e ABORTADO — porque a Regra 5 exige foto + legenda
# como uma unidade so. Ou seja: sem pywin32 o WhatsApp nao publica nada, e
# a unica pista e um `log.warning("clipboard CF_HDROP: ...")`.
# So Windows: no servidor Linux (deploy/) quem envia e a Evolution API.
pywin32; sys_platform == "win32"

# core/trending.py — assuntos em alta. Tem fallback, mas sem ele a
# funcionalidade some silenciosamente.
pytrends

# integrations/amazon_paapi.py importa `amazon_creatorsapi`, que e um
# pacote DIFERENTE de `python-amazon-paapi` (ja listado acima).
amazon-creatorsapi

